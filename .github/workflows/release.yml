name: release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

# INF-01: single source of truth for the hawcx-manager crate version that
# this SDK release line ships. The SDK release *tag* (v0.1.0-alpha.N) and
# the bundled crate version are decoupled by design, so we pin the crate
# version explicitly here rather than deriving it from the (independent)
# git tag. Bump this in lockstep with the hawcx-manager release in
# hx_agent_client_auth_service (workspace version), and keep CLAUDE.md and
# release-node.yml / release-python.yml in sync. The committed Cargo.lock
# (see ./Cargo.lock) makes `--locked` meaningful for the transitive graph;
# this `--version =<exact>` pin makes the top-level crate deterministic.
#
# 2026-09-04: 0.8.8 → 0.11.8. 0.8.8 dates to the CAS workspace bump of
# 2026-06-11 and predates hx_agent_client_auth_service #434 (2026-08-30),
# which is what makes the unseal-orchestrator reachable inside the
# multi-call image at all: the `haap-unseal-orch` argv[0] route, the
# `[orchestrator] multicall_bin` field, and the `check_exec_targets` boot
# pre-flight all landed in that one commit, at workspace 0.10.21. At 0.8.8
# `dispatch::role_from_argv0` has exactly seven arms and no orchestrator
# one, so the `haap-unseal-orch` entry added to LEGACY_NAMES below would
# resolve to `Role::Direct` and print help instead of running the role —
# a silently wrong start rather than a spawn error. The pin and the
# symlink are therefore one change, not two.
env:
  HAWCX_MANAGER_VERSION: "0.11.8"

jobs:
  build:
    name: build ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-22.04
          - target: aarch64-unknown-linux-gnu
            os: ubuntu-22.04-arm
          - target: aarch64-apple-darwin
            os: macos-14
          - target: x86_64-pc-windows-msvc
            os: windows-latest
          - target: aarch64-pc-windows-msvc
            os: windows-latest
            # Cross-compiled on an x86_64 Windows runner (no native ARM64
            # Windows runner here): the binary can't be executed, so the
            # version-assert skips the exec check (mirrors release-node.yml).
            cross: true

    steps:
      - name: Checkout hx_agentic_sdk
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      # F-2 sibling-checkout migration (2026-05-27): hawcx-manager is
      # no longer built in-pipeline from a sibling clone of
      # hx_agent_client_auth_service. It's published to the private
      # Kellnr at cargo.hawcx.com on tag push and installed here via
      # `cargo install hawcx-manager --registry hawcx --locked`. This
      # mirrors release-node.yml's build-binary job.
      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8 # stable branch head
        with:
          targets: ${{ matrix.target }}

      - name: Install protoc
        uses: arduino/setup-protoc@c65c819552d16ad3c9b72d9dfd5ba5237b9c906b # v3.0.0
        with:
          version: '25.x'
          repo-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Install Linux system deps (libdbus for keyring)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libdbus-1-dev pkg-config

      - name: Configure hawcx cargo registry
        shell: bash
        env:
          CARGO_HAWCX_TOKEN: ${{ secrets.CARGO_HAWCX_TOKEN }}
        run: |
          mkdir -p ~/.cargo
          cat >> ~/.cargo/config.toml <<'EOF'
          [registries.hawcx]
          index = "sparse+https://cargo.hawcx.com/api/v1/crates/"

          [registry]
          global-credential-providers = ["cargo:token"]
          EOF
          cat > ~/.cargo/credentials.toml <<EOF
          [registries.hawcx]
          token = "${CARGO_HAWCX_TOKEN}"
          EOF
          chmod 600 ~/.cargo/credentials.toml

      # Exact-match binary cache, keyed by tag + target. No restore-keys
      # fallback — a stale binary from a prior tag must never be
      # silently reused for a new release.
      - name: Restore cached hawcx-manager binary
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ${{ runner.temp }}/staging/bin
          # The pinned version is IN the key: a stale entry built for a
          # previous HAWCX_MANAGER_VERSION can no longer be restored under a
          # new pin (which would ship the old binary under the new tag).
          # Derived rather than a hand-bumped suffix so it cannot be
          # forgotten on the next bump. Cf. release-node.yml's `-bins2`.
          key: hawcx-manager-${{ env.HAWCX_MANAGER_VERSION }}-${{ matrix.target }}-${{ github.ref_name }}

      # INF-01: pin the exact crate version (--version =X.Y.Z) so the
      # binary stamped under this SDK tag is deterministically tied to a
      # single hawcx-manager source version — NOT "whatever the Kellnr
      # index currently serves as highest non-yanked". `--locked` makes
      # the transitive graph reproducible against the crate's published
      # Cargo.lock; the `=` version constraint makes the top-level crate
      # reproducible. A new (legitimate-but-untested, or malicious) publish
      # to cargo.hawcx.com can no longer be silently pulled into a build of
      # an already-evaluated tag.
      - name: Install hawcx-manager from hawcx registry
        if: steps.cache.outputs.cache-hit != 'true'
        shell: bash
        run: |
          set -euo pipefail
          cargo install hawcx-manager \
            --registry hawcx \
            --version "=${HAWCX_MANAGER_VERSION}" \
            --target ${{ matrix.target }} \
            --root "${RUNNER_TEMP}/staging" \
            --locked

      # INF-01: assert the installed binary reports the version we pinned.
      # A mismatch means the registry served something other than what we
      # asked for (index tampering, yank/replace race) — fail loudly rather
      # than ship an unexpected binary under this tag.
      - name: Assert installed hawcx-manager version
        if: steps.cache.outputs.cache-hit != 'true'
        shell: bash
        run: |
          set -euo pipefail
          BIN="${RUNNER_TEMP}/staging/bin/hawcx-manager"
          [[ "${{ runner.os }}" == "Windows" ]] && BIN="${BIN}.exe"
          if [[ ! -s "$BIN" ]]; then
            echo "::error::hawcx-manager binary missing or empty at $BIN"
            exit 1
          fi
          # Cross-compiled targets (e.g. aarch64-pc-windows-msvc built on an
          # x86_64 runner) cannot be executed here. The exact version is already
          # guaranteed by `cargo install --version="=X.Y.Z" --locked` above, so
          # assert the artifact exists and skip the exec-based check.
          if [[ "${{ matrix.cross }}" == "true" ]]; then
            echo "cross-compiled ${{ matrix.target }}: artifact present ($(wc -c < "$BIN") bytes); skipping exec-based version check"
            exit 0
          fi
          got="$("$BIN" --version | grep -oE '[0-9]+\.[0-9]+\.[0-9]+([-.][0-9A-Za-z.]+)?' | head -n1)"
          echo "installed hawcx-manager version: ${got} (expected ${HAWCX_MANAGER_VERSION})"
          if [[ "${got}" != "${HAWCX_MANAGER_VERSION}" ]]; then
            echo "::error::hawcx-manager version mismatch: got '${got}', expected '${HAWCX_MANAGER_VERSION}'"
            exit 1
          fi

      - name: Save hawcx-manager binary to cache
        if: steps.cache.outputs.cache-hit != 'true' && success()
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ${{ runner.temp }}/staging/bin
          # The pinned version is IN the key: a stale entry built for a
          # previous HAWCX_MANAGER_VERSION can no longer be restored under a
          # new pin (which would ship the old binary under the new tag).
          # Derived rather than a hand-bumped suffix so it cannot be
          # forgotten on the next bump. Cf. release-node.yml's `-bins2`.
          key: hawcx-manager-${{ env.HAWCX_MANAGER_VERSION }}-${{ matrix.target }}-${{ github.ref_name }}

      - name: Package distribution tarball
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p dist
          OUT="hx-agent-sdk-${{ github.ref_name }}-${{ matrix.target }}"
          mkdir -p "$OUT/bin"
          # client_auth_service agent-side runtime: supervisor + 5
          # protected children + the haap-sdk demo CLI all dispatched
          # off a single multi-call binary (hawcx-manager) per CS
          # v7.2.5 §35.6 + §39.12 and the Phase 3-5 cutover memo at
          # /hx_agent_canonical_spec/DESIGN-MEMO-MULTICALL-BINARY.md.
          #
          # 2026-05-22 (Phase 3-5): one real binary + 8 legacy aliases.
          # Unix → symlinks; Windows → .exe copies (symlinks require
          # admin/dev-mode and are not MDM-friendly).
          LEGACY_NAMES="haap-authenticator haap-tqs-precompute haap-tqs-jit haap-assembler haap-eib haap-supervisor haap-sdk haap-unseal-orch"
          BIN_DIR="${RUNNER_TEMP}/staging/bin"
          if [[ "${{ runner.os }}" == "Windows" ]]; then
            cp "$BIN_DIR/hawcx-manager.exe" "$OUT/bin/"
            for n in $LEGACY_NAMES; do
              cp "$OUT/bin/hawcx-manager.exe" "$OUT/bin/$n.exe"
            done
          else
            cp "$BIN_DIR/hawcx-manager" "$OUT/bin/"
            (cd "$OUT/bin" && for n in $LEGACY_NAMES; do
              ln -sf hawcx-manager "$n"
            done)
          fi
          cp README.md "$OUT/" 2>/dev/null || true
          cp -r docs "$OUT/" 2>/dev/null || true
          tar -czf "dist/$OUT.tar.gz" "$OUT"

      - name: Upload distribution artifact (release asset)
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: hx-agent-sdk-${{ matrix.target }}
          path: dist/*

      - name: Stage Docker binaries (flat layout)
        if: contains(matrix.target, 'linux-gnu')
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p docker-staging
          # Phase 3-5 cutover (2026-05-22): only the real binary
          # (hawcx-manager) is uploaded as the docker-bins artifact.
          # actions/upload-artifact (v7) dereferences symlinks when
          # zipping; the 8 legacy aliases are regenerated on the docker
          # job runner BEFORE the build context is assembled.
          cp "${RUNNER_TEMP}/staging/bin/hawcx-manager" docker-staging/
          ls -la docker-staging/

      - name: Upload Linux binaries for Docker (linux only)
        if: contains(matrix.target, 'linux-gnu')
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: docker-bins-${{ matrix.target }}
          path: docker-staging/

  docker:
    name: docker ${{ matrix.platform }}
    runs-on: ${{ matrix.runner }}
    needs: build
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: linux/amd64
            arch_suffix: amd64
            target: x86_64-unknown-linux-gnu
            runner: ubuntu-22.04
          - platform: linux/arm64
            arch_suffix: arm64
            target: aarch64-unknown-linux-gnu
            runner: ubuntu-22.04-arm
    steps:
      - name: Checkout hx_agentic_sdk
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Download pre-built Linux binaries
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: docker-bins-${{ matrix.target }}
          path: ./prebuilt-bins

      - name: Make binaries executable + generate legacy symlinks
        shell: bash
        run: |
          set -euo pipefail
          chmod +x ./prebuilt-bins/hawcx-manager
          # Phase 3-5 cutover (2026-05-22): regenerate the 8 legacy-name
          # symlinks here because actions/upload-artifact (v7) dereferences
          # symlinks when zipping (so they couldn't survive the matrix-job
          # → docker-job artifact roundtrip). Building them here means
          # the Dockerfile.fast `COPY prebuilt-bins/ /usr/local/bin/`
          # step lands a real binary + 8 symlinks under one COPY, and
          # distroless never needs to run a shell.
          (cd ./prebuilt-bins && for n in haap-authenticator haap-tqs-precompute haap-tqs-jit haap-assembler haap-eib haap-supervisor haap-sdk haap-unseal-orch; do
            ln -sf hawcx-manager "$n"
          done)
          ls -la ./prebuilt-bins/

      - name: Set up Buildx
        uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e # v4.3.0

      - name: Login to GHCR
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4.6.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # INF-03: emit SLSA provenance + an SBOM attestation for the image,
      # so a consumer can attest the build-to-source chain. `mode=max`
      # records the full build invocation (materials + builder identity).
      - name: Build + push single-arch image
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.3.0
        with:
          context: .
          file: Dockerfile.fast
          platforms: ${{ matrix.platform }}
          push: true
          provenance: mode=max
          sbom: true
          tags: |
            ghcr.io/hawcx/hx-agent-sdk:${{ github.ref_name }}-${{ matrix.arch_suffix }}
          cache-from: type=gha,scope=${{ matrix.arch_suffix }}
          cache-to: type=gha,scope=${{ matrix.arch_suffix }},mode=max

  docker_manifest:
    name: docker multi-arch manifest
    runs-on: ubuntu-latest
    needs: docker
    # INF-03: id-token for cosign keyless (Fulcio/Rekor) signing of the
    # published manifest; packages:write so cosign can attach the
    # signature to the GHCR repo.
    permissions:
      packages: write
      id-token: write
    steps:
      - name: Login to GHCR
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4.6.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # Compute the moving dist-tag for this release: prereleases
      # (alpha/beta/rc) move the matching channel tag (e.g. `alpha`),
      # GA releases move `latest`. This mirrors the npm dist-tag
      # discipline in release-node.yml so a customer pulling
      # `ghcr.io/hawcx/hx-agent-sdk:alpha` always lands on the latest
      # alpha image. NEVER point `:latest` at a prerelease — that's
      # how an evaluator accidentally ships unreleased code.
      - name: Compute moving dist-tag
        id: dist
        shell: bash
        env:
          REF_NAME: ${{ github.ref_name }}
        run: |
          set -euo pipefail
          case "$REF_NAME" in
            *alpha*) echo "moving_tag=alpha" >> "$GITHUB_OUTPUT" ;;
            *beta*)  echo "moving_tag=beta"  >> "$GITHUB_OUTPUT" ;;
            *rc*)    echo "moving_tag=rc"    >> "$GITHUB_OUTPUT" ;;
            *)       echo "moving_tag=latest" >> "$GITHUB_OUTPUT" ;;
          esac

      - name: Create + push multi-arch manifest
        run: |
          docker buildx imagetools create \
            -t ghcr.io/hawcx/hx-agent-sdk:${{ github.ref_name }} \
            -t ghcr.io/hawcx/hx-agent-sdk:${{ steps.dist.outputs.moving_tag }} \
            ghcr.io/hawcx/hx-agent-sdk:${{ github.ref_name }}-amd64 \
            ghcr.io/hawcx/hx-agent-sdk:${{ github.ref_name }}-arm64

      - name: Verify manifest (per-tag + multi-arch platforms)
        run: |
          set -euo pipefail
          docker buildx imagetools inspect ghcr.io/hawcx/hx-agent-sdk:${{ github.ref_name }}
          # Fail loudly if either platform is missing from the
          # multi-arch index — alpha.10 silently shipped without this
          # check, and an arm64 regression would be invisible until a
          # customer on Apple Silicon hit `exec format error`.
          platforms=$(docker buildx imagetools inspect \
            ghcr.io/hawcx/hx-agent-sdk:${{ github.ref_name }} \
            --format '{{range .Manifest.Manifests}}{{.Platform.OS}}/{{.Platform.Architecture}} {{end}}')
          echo "platforms: $platforms"
          for p in linux/amd64 linux/arm64; do
            case " $platforms " in
              *" $p "*) ;;
              *) echo "::error::manifest missing platform $p"; exit 1 ;;
            esac
          done

      # INF-03: keyless-sign the published multi-arch image. We sign the
      # immutable manifest DIGEST (not a mutable tag) so the signature
      # binds the exact bytes, then customers verify against this repo's
      # workflow OIDC identity. See docs/RELEASE.md for the verify command.
      - name: Install cosign
        uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6 # v4.1.2

      - name: Resolve manifest digest
        id: digest
        run: |
          set -euo pipefail
          digest=$(docker buildx imagetools inspect \
            ghcr.io/hawcx/hx-agent-sdk:${{ github.ref_name }} \
            --format '{{json .Manifest.Digest}}' | tr -d '"')
          echo "digest=$digest" >> "$GITHUB_OUTPUT"
          echo "image digest: $digest"

      - name: Cosign sign image (keyless)
        env:
          COSIGN_YES: "true"
        run: |
          set -euo pipefail
          cosign sign --recursive \
            "ghcr.io/hawcx/hx-agent-sdk@${{ steps.digest.outputs.digest }}"

  # Read the image that was just pushed and assert the supervisor can
  # actually reach the unseal orchestrator in it. Both platforms, because
  # the two arch images are built by separate matrix legs and a symlink
  # loop that ran on only one of them would be invisible in the manifest.
  #
  # This exists because nothing else looks at this image. `bundle_smoke_test`
  # below runs the docker/bundle compose, which has no hx-agent-sdk service
  # at all (hx-caa x2, haap-rsv, redis), and ci.yml never touches it — so
  # v0.1.4 shipped with the orchestrator unreachable and every check green.
  #
  # Registry reads, not `docker run`: no daemon needed, and it inspects the
  # bytes that were actually pushed rather than the build context that was
  # meant to produce them.
  image_orch_target:
    name: image carries an orchestrator exec target
    runs-on: ubuntu-latest
    needs: [docker_manifest]
    permissions:
      packages: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Assert haap-unseal-orch is reachable in the image
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          for arch in amd64 arm64; do
            echo "=== linux/$arch ==="
            ./scripts/check-image-orch-target.py \
              "ghcr.io/hawcx/hx-agent-sdk:${{ github.ref_name }}" "$arch"
          done

  bundle_smoke_test:
    name: bundle smoke test
    runs-on: ubuntu-latest
    needs: [docker_manifest]
    # The bundle pulls the CAA image (published from
    # hx_agent_client_admin_service on its own release cadence) plus RSV and
    # redis. smoke-test.sh self-skips (exit 0 with explanation) when the
    # matching hx-caa:${HAAP_VERSION} manifest is absent, so a red here
    # means a real structural breakage (compose syntax, entrypoint, env
    # handling) — NOT "we haven't tagged CAA yet."
    #
    # 2026-09-04: this comment used to claim the bundle pulls "both the SDK
    # image (just published above) and the CAA image". It does not — there
    # is no hx-agent-sdk service in docker/bundle/docker-compose.yml. That
    # wrong sentence is a good part of why the SDK image went four releases
    # with an unreachable orchestrator and nobody saw it; the
    # `image_orch_target` job above is the coverage it implied.
    if: startsWith(github.ref, 'refs/tags/')
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Login to GHCR
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4.6.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Run bundle smoke test
        env:
          HAAP_VERSION: ${{ github.ref_name }}
          CI: "1"
        run: |
          cd docker/bundle
          ./smoke-test.sh

  github_release:
    name: create GitHub release
    runs-on: ubuntu-latest
    needs: build
    if: startsWith(github.ref, 'refs/tags/')
    # INF-03: id-token for cosign keyless blob signing of the tarballs +
    # SHA256SUMS. contents:write to attach the release assets.
    permissions:
      contents: write
      id-token: write
    steps:
      - name: Download all distribution artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          path: ./release-assets
          pattern: "hx-agent-sdk-*"
          merge-multiple: true

      - name: List downloaded assets (diagnostic)
        run: ls -lah ./release-assets/

      # INF-03: emit a SHA256SUMS manifest over every release tarball, then
      # cosign keyless-sign that manifest. Signing the checksum file (rather
      # than each tarball) transitively binds all artifacts: verify the
      # SHA256SUMS signature, then `sha256sum -c` the tarballs against it.
      - name: Generate SHA256SUMS
        shell: bash
        run: |
          set -euo pipefail
          cd ./release-assets
          # Deterministic ordering so the manifest is reproducible.
          find . -maxdepth 1 -type f -name '*.tar.gz' -printf '%P\n' | sort \
            | xargs -r sha256sum > SHA256SUMS
          echo "=== SHA256SUMS ==="
          cat SHA256SUMS

      - name: Install cosign
        uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6 # v4.1.2

      - name: Cosign sign SHA256SUMS (keyless)
        env:
          COSIGN_YES: "true"
        shell: bash
        run: |
          set -euo pipefail
          cd ./release-assets
          # Emits SHA256SUMS.sig (signature) + SHA256SUMS.pem (cert chain),
          # both attached to the release for offline verification.
          cosign sign-blob \
            --output-signature SHA256SUMS.sig \
            --output-certificate SHA256SUMS.pem \
            SHA256SUMS

      - name: Create GitHub Release
        uses: softprops/action-gh-release@efb35369e0ad2afab669f228072c1b0d510eae64 # v3.0.3
        with:
          tag_name: ${{ github.ref_name }}
          name: ${{ github.ref_name }}
          body: |
            HAAP SDK release ${{ github.ref_name }}.

            ## Distribution

            **Binary tarballs** (attached below):
            - Linux x86_64 / aarch64
            - macOS aarch64 (Apple Silicon)
            - Windows x86_64 / aarch64

            **Multi-arch Docker image** (supports `linux/amd64` and
            `linux/arm64`):
            ```
            ghcr.io/hawcx/hx-agent-sdk:${{ github.ref_name }}
            ```
            Prereleases (alpha/beta/rc) also move the matching channel
            tag (`:alpha`, `:beta`, `:rc`). `:latest` is reserved for
            GA releases.

            ## Contents

            **Multi-call binary** (Phase 3-5 cutover, 2026-05-22): the
            tarball / image now ships ONE real binary, `hawcx-manager`,
            with 8 legacy-name symlinks (Unix) or .exe copies (Windows)
            that dispatch by argv[0] to the corresponding role:

            - `haap-supervisor` → `hawcx-manager supervisor` (lifecycle, zero crypto material)
            - `haap-authenticator` → `hawcx-manager authenticator` (holds IK_i, X3DH per §4.2.1)
            - `haap-tqs-precompute` → `hawcx-manager tqs-precompute` (session-scoped K_session_root per §40)
            - `haap-tqs-jit` → `hawcx-manager tqs-jit` (request-scoped, paired 1:1 with Assembler)
            - `haap-assembler` → `hawcx-manager assembler` (single-flight crypto-proxy per §39)
            - `haap-eib` → `hawcx-manager eib` (External Identity Broker, OAuth bearer per §45)
            - `haap-sdk` → `hawcx-manager sdk` (demo / debug CLI)
            - `haap-unseal-orch` → `hawcx-manager unseal-orch` (§35.4 K_unseal release broker)

            Existing customer scripts that invoke `haap-supervisor`,
            `haap-authenticator`, etc. continue to work unchanged — the
            symlinks preserve every legacy call site. Direct invocation
            via `hawcx-manager <subcommand>` is also supported.

            On Windows, supervisor↔child IPC uses Named Pipes with DACL
            restricting access to the current user SID + SYSTEM, per
            CS v7.2.5 §39.12.2. On Linux / macOS, IPC uses Unix domain
            sockets with `SO_PEERCRED` / `LOCAL_PEERCRED`, per §39.12.1.

            **MCP server-side sidecar** (`haap-rsv`) ships from a
            separate image as of 2026-05-21:
            `ghcr.io/hawcx/haap-rsv:${{ github.ref_name }}`. Source lives
            in `hx_agent_authorizer/crates/haap-rsv-bin`. It is Unix-only
            by design (UDS + peer-credential checks for the
            local-sidecar trust model per §39.12.1).

            ## Evaluation bundle

            A Docker Compose bundle that spins up CAA + RSV together for local
            evaluation is available at `docker/bundle/`:

            ```bash
            git clone -b ${{ github.ref_name }} https://github.com/hawcx/hx_agentic_sdk
            cd hx_agentic_sdk/docker/bundle
            cp .env.example .env
            # Edit .env with tenant credentials
            docker compose up
            ```

            See [docker/bundle/README.md](https://github.com/hawcx/hx_agentic_sdk/blob/${{ github.ref_name }}/docker/bundle/README.md) for details.

            ## Status

            Pre-product alpha release. Customer evaluation only. Production deployment not yet recommended.

            See [build workflow run](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}) for build details.
          files: ./release-assets/**/*
          draft: false
          prerelease: ${{ contains(github.ref_name, 'alpha') || contains(github.ref_name, 'beta') || contains(github.ref_name, 'rc') }}
          fail_on_unmatched_files: false
